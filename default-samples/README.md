# Default Project Samples

This directory contains tracked default sample source files used to seed a better first-run experience.

These files are not runtime storage.
They are copied into project storage only when the system detects that the corresponding default sample documents or outputs are missing.
