# Include these timeout values in the server/location that proxies
# public /api/ traffic to the web service on 127.0.0.1:3002.

proxy_connect_timeout 30s;
proxy_send_timeout 300s;
proxy_read_timeout 300s;
send_timeout 300s;
